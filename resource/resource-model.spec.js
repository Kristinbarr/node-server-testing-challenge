// testing DB models
const Resources = require('./resource-model')
const db = require('../data/dbConfig')

describe('findAll()', () => {
  it('should return all resources', () => {})
  it('should return all resources', () => {})
  it('should return all resources', () => {})
})
